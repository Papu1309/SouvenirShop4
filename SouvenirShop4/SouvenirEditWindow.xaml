<Window x:Class="SouvenirShop4.SouvenirEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SouvenirShop4"
        mc:Ignorable="d"
        Title="SouvenirEditWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название товара:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Name="txtName" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="2" Text="Описание:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Name="txtDescription" Margin="0,0,0,10" Padding="5" Height="60" 
                 TextWrapping="Wrap" AcceptsReturn="True"/>

        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Цена:" Margin="0,0,0,5"/>
                <TextBox Name="txtPrice" Padding="5"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Количество:" Margin="0,0,0,5"/>
                <TextBox Name="txtStockQuantity" Padding="5"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="5" Text="Категория:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="6" Name="cmbCategories" DisplayMemberPath="CategoryName" 
                  Margin="0,0,0,20" Padding="5"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="CancelButton_Click" Margin="0,0,10,0" 
                   Padding="15,5"/>
            <Button Content="Сохранить" Click="SaveButton_Click" 
                   Background="#FF0080FF" Foreground="White" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>
